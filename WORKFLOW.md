# F1 Data Collection Workflow (2026+)

本文档将运维任务按周期划分为：**赛前准备**（准备赛历与同步）与 **赛中/赛后处理**（仅成绩获取）。

---

## 📅 1. 赛前任务 (Pre-Race / Setup)
*目标：获取赛历、补全数据、并同步到前端。*

1. **采集与增强数据**:
   ```bash
   python scraper.py  # 采集原始数据
   python research/final_refine_2026.py  # 补全时区 (gmtOffset)、赛道图及头像配置
   ```
2. **资产标准化**:
   ```bash
   python maintenance/standardize_svgs.py  # 标准化国旗 (56x56)
   ```
3. **分发至前端**:
   ```bash
   python sync_data.py --schedule-only  # 将最终 schedule_2026.json 同步至前端项目
   ```

---

## 🏁 2. 赛中/赛后任务 (During-Race / Results)
*目标：仅处理比赛即时成绩。*

1. **获取最新成绩**:
   - 运行结果采集脚本（如 `scraper_results_2026.py`）。
2. **入库与同步**:
   ```bash
   python sync_data.py --results-only  # 将成绩正式注入前端 f1.db (SQLite)
   ```

---

## 🛠️ 核心逻辑速查

### 资产展示标准 (Frontend Rules)
- **国旗**: 14x14 圆形展示。
- **赛道图**: 暗色模式应用 `dark:invert dark:brightness-150`。
- **车手肖像**: 锁定 `scale-1.4` + `origin-top`。

---

## 🚀 紧急恢复流程
`refine_2026.py` -> `sync_data.py`
